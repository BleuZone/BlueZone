const {userMethods, usernameMethods, postMethods, pageMethods, commentMethods } = require('../Models/FunctionExports.js');


console.log(userMethods);

const addUser = (req, res) => {
  const id = req.params.id;


}

